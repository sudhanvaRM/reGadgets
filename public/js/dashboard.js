// Example JavaScript code for interacting with the dashboard

// Fetch and display dashboard statistics
function fetchDashboardStats() {
    // Make API call to fetch dashboard statistics
    // Update the "dashboard-stats" div with the received data
  }
  
  // Fetch and display product table data
  function fetchProductTableData() {
    // Make API call to fetch product table data
    // Update the "product-table" with the received data
  }
  
  // Fetch and display order table data
  function fetchOrderTableData() {
    // Make API call to fetch order table data
    // Update the "order-table" with the received data
  }
  
  // Fetch and display customer table data
  function fetchCustomerTableData() {
    // Make API call to fetch customer table data
    // Update the "customer-table" with the received data
  }
  
  // Call
  